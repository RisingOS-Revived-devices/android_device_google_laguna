hal_client_domain(system_app, hal_fingerprint)

set_prop(system_app, fingerprint_sp_prop)

# Allow PixelExtraParts to traverse the display sysfs path behind
# /sys/class/backlight/panel0-backlight.
allow system_app sysfs_display:dir search;
